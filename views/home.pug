extends layouts/main 
include mixins/videoBlock

block contents
    .home-videos
        each video in videos
            + videoBlock({
                id : video.id,
                title : video.title,
                views : video.views,
                videoFile : video.fileUrl
            })